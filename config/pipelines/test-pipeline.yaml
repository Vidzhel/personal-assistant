name: test-pipeline
description: Updated description
version: 1
trigger:
  type: manual
nodes:
  step-one:
    skill: digest
    action: compile-briefing
    params: {}
  step-two:
    skill: telegram
    action: send-message
    params: {}
connections:
  - from: step-one
    to: step-two
enabled: true
