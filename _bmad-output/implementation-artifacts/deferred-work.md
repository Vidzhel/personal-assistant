# Reliability follow-ups — September 2026

The active delivery queue is
[file-first-completion-2026-09-05.md](file-first-completion-2026-09-05.md).
The owner subsequently confirmed Raven has not been used: legacy runtime data
may be discarded. Legacy export/migration and graph restoration requirements
below are superseded by that decision; historical incident evidence remains.
Items assigned to an R-task below are scheduled within this pass, not accepted
as permanent limitations. Update the status and evidence when each is resolved.

| Item | State / target | Resolution and verification |
| --- | --- | --- |
| save_artifact reports success without writing an artifact | Stub removed in F2; delivery scheduled W1 | The old tool only emitted a progress event and had no persistence consumer. Remove it from advertised task tools; agents can use their actual filesystem tools and report paths in completion. W1 adds real project/repository artifact persistence, registered links and mobile previews with a command→file→Git→browser acceptance journey. |
| Chat/session UI races, optimistic rejected text, disconnected sends | Complete R5 | Session-scoped state/drafts, correlated acceptance and recoverable rejection, and reconnect history/task reconciliation. Eleven browser journeys include switching, failed sends and a missed completion. |
| References stream stops updating after the 201-message buffer fills | Complete R5 | References now use the reviewed identity cursor and cancel stale session loads; rollover/extraction regressions pass. |
| Intentional WebSocket disconnect schedules a reconnect | Complete R5 | Close-state guard/timer cleanup prevent orphan reconnects; focused transport and browser reconnect tests pass. |
| Voice transcript output depends on process working directory | Complete R4 | Output uses the configured runtime root captured at startup. Real temp-file tests from a different CWD and across restart pass; no Gemini requests used. |
| Voice/file transcription can write and emit after service stop | Complete R5 | Per-start lifetime and abort controllers own processing waits and suppress late outputs; deferred fake upload/poll/inference/deletion, timeout and restart regressions pass (26 voice tests). Provider-side cleanup is a separate follow-up below. |
| Maintenance convention audit still inspects retired agents.json/schedules.json | Complete R5 | Audits current project/library definitions with their real validators, including newly activated skill bindings. Current-layout, hot-reload and maintenance lifecycle regressions pass. |
| IMAP service registration requires OAuth variables instead of its actual IMAP credentials | Complete R5 | Registration now uses GMAIL_IMAP_USER/GMAIL_IMAP_PASSWORD; fake boundary tests prove eligibility. Isolated email tests do not connect to IMAP. |
| Active agent tasks can outlive core shutdown | Complete R5 | Manager stops admission, settles queued/running and approved work, and emits canonical terminal state before disposal. Orchestrator, retrospective, heartbeat and consolidation own their lifetimes; held-backend and local-memory-write regressions pass. External work and already admitted MCP mutation boundaries remain explicit below. |
| Failed or partial memory consolidation consumed pending candidates | Complete R5 | Invalid/failed model results, rejected/thrown operations, operation caps, budget failures and index failures now retain candidate files. Successful application and index generation precede archival; actual archive outcomes are counted. Tests cover partial retry and admitted-write shutdown drain. |
| Gemini uploads/inference after client cancellation or deletion failure | Provider cleanup follow-up | Abort signals stop local waits and outputs, but do not prove remote inference stopped or uploaded files disappeared. Persist Raven correlation IDs and known remote file IDs in operational storage, retry exact-file cleanup through existing maintenance, test restart/timeout/delete failure with fake SDK, then use an explicitly authorized provider canary to verify server retention. The provider FAILED-before-inference case is already cleaned up and tested. |
| Global knowledge view and implicit chat project selection | Workspace-context follow-up | GraphChatPanel now uses real shared chat, but KnowledgePage selects the first project and graph reads are global. Add an explicit existing-project selector and server-side project filtering together with the workspace context contract; test reordered project lists, project switching and selected-node ownership. Do not infer project isolation from the current global graph view. |
| Already admitted in-process MCP mutations can outlive task cancellation | Explicit deferred mutation-drain work | R5 guards registration in `packages/core/src/mcp-server/{index,memory-mcp,guard-tool}.ts` and checks `agent-manager/agent-session.ts` permission results before and after awaits, so cancelled tasks cannot invoke a new local tool or receive a late success response. This does not cancel or roll back an already admitted handler: scaffold/registry writes in `mcp-server/tools/{scaffold,system}.ts`, memory writes in `agent-memory/memory-store.ts`, and knowledge/task store calls may complete after abort. Track admitted handler promises per task, pass cancellation through storage methods, define each commit boundary and shutdown drain policy, and test held file, graph and SQLite mutations during cancellation/stop before claiming full drain. Current `mcp-tool-cancellation.test.ts` proves entry rejection and response suppression only. |
| Workspace attachments and project-owned memory | Authorized after F1–F9 | Review dissertation and teaching first; implement flexible project layouts, direct repository shell/file/Git work, context-index links and mobile artifact access. The active queue records the owner’s full requirements; no repository embedding pipeline or legacy migration is requested. |
| Graph replacement | Deferred with workspace design | Design file-owned relationships and project membership, switch readers and remove predecessor infrastructure. No legacy export/migration is required under the updated owner decision. |
| Prior local Neo4j test-contact delta | Restoration waived by owner | R3 source audit established that the earlier successful startup included a destructive Bubble reindex query; the unsupported no-deletion statement was corrected. Exact prior impact remains unknown. The owner subsequently confirmed the project was unused and legacy runtime state may be discarded; this closes recovery work without claiming restoration. Default tests block the real graph-client factory. |
| Project definitions depend on stale SQLite settings/IDs | Complete F1; migration waived | Current files determine settings and identity; plain files use path IDs/defaults. SQL name matching and orphan scaffolding are removed. Reconstruction, external edits, historical tombstones, explicit system permissions and startup/failed-reload identity rejection pass. |
| Approved-action redispatch loses project ownership and tolerates invalid capability bindings | F9 dispatch cleanup | `AgentManager.executeAction` carries the session ID but omits its project ID, so these records currently use the system folder. Resolve project ownership from the trusted session before admission, reject missing configured capability bindings rather than catching them into empty capabilities, and remove the obsolete suite fallback comments. Verify the approval HTTP journey retains project ownership in YAML/SSE and that invalid bindings fail before model dispatch without creating another approval loop. |
| Process interruption between project filesystem and SQLite changes | Recovery follow-up | Add startup detection for missing managed paths and unmatched archive snapshots, report conflicts without deleting data, then exercise process-kill fault injection around each mutation boundary and a deterministic restore/complete operation. Current compensation covers handled failures; no cross-store power-loss atomicity is claimed. |
| Knowledge files and graph disagree after interrupted CRUD or manual file removal | Recovery follow-up | Preserve durable files and unmatched graph records; reject stale file identity claims in CRUD. Add a read-only reconciliation report with file/graph IDs and paths, backed-up explicit repair choices, and interruption tests around graph/file mutation boundaries. A graph deletion already in flight at shutdown may complete while its file is retained for recovery. Do not automatically prune unmatched records. |
| Externally edited knowledge files retain prior embeddings/chunks during routine reindex | Retrieval follow-up | Add content hashes and change-aware derived-index refresh while preserving durable graph links and project memberships; test edited-file retrieval and failed refresh retries. Existing explicit reindex-embeddings endpoint regenerates chunks; routine reindex currently refreshes file fields/tags and preserves graph metadata. |
| GitHub reported dependency alerts during checkpoint push | Complete R6; maintain scoped override | Compatible dependency fixes plus the reviewed Transformers→Sharp 0.35.4 override leave zero npm audit advisories. See docs/assessments/2026-09-05-dependency-review.md for exact versions, npm override guard and real embedding/native validation. Remove the override only after upstream accepts a patched version and clean-install/native/build regressions pass. GitHub's aggregated alert count can lag the reviewed lockfile. |
| Live Claude/account canary | Separate from isolated verification | Use an explicitly chosen disposable task/account destination after deterministic tests; verify authentication, SDK resume, MCP scope and delivery with recorded outcomes. No outbound messages are authorized by this testing pass. |
| Legacy global daily budget setting has no runtime enforcement | Explicit budget follow-up | `RAVEN_MAX_BUDGET_USD_PER_DAY` is parsed but unused; documentation and examples no longer claim a global cap. Define how CLI subscription usage and API costs are counted, record usage/reservations in existing operational SQLite, enforce admission across chat/task/heartbeat/learning paths, and test concurrent reservations, rollover timezone, unknown costs and restart before advertising a daily limit. Existing per-turn and intent budgets are narrower controls. |
| Ordinary schedules can remain never-fired or stale without self-test failure | Schedule health follow-up | Extend services/system/self-test.ts using enabled definitions from the existing ScheduleEngine and schedule_fires. Compare expected cron windows with explicit startup/activation grace; preserve disabled and recent in-flight schedules and canary-specific checks. Test fake-clock/temp-DB never-fired, stale success, recent/in-flight, disabled, reload/newly-enabled and timezone cases. Current general checks inspect recorded statuses; only the weekly canary checks absence/staleness. No new scheduler is needed. |

No issue is closed merely because a fake model returned a successful result.
Record what the automated boundary actually exercised.
