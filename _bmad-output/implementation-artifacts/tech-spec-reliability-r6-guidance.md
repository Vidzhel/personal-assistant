---
title: Reliable development guidance and dependency baseline
status: complete
baseline_commit: 9c2de3c
execution_mode: plan-code-review
---

The owner authorized finishing existing reliability tasks, reviewing/testing each
checkpoint, and committing and pushing throughout. Repository attachments,
project-memory layout and graph replacement remain deferred. This is R6 of the
canonical reliability-completion-2026-09-05.md queue.

## Outcome and scope

Both Claude and Codex should receive current, consistent development instructions.
Historical plans should no longer direct a developer to recreate deleted engines
or reopen work already shipped. Commands and architecture claims must describe
the tested code. Dependency alerts must be fixed within a justified compatible
change or retained with an explicit exposure assessment and resolution plan.

## Implementation

1. Reconcile AGENTS.md, CLAUDE.md, README.md, ARCHITECTURE.md and BMAD's active
   project-context.md against R0–R5. Explain explicit capability scope, managed
   file definitions, SQLite operational state, preserved graph relationships,
   temporary verification roots and the unchanged Claude SDK runtime provider.
2. Preserve historical planning evidence but label superseded August/March
   trackers and draft workspace documents. Point to the one current queue.
   Record intentional heartbeat/canary deviations without reopening old designs.
3. Finish the reviewed Codex browser skill/agent migration and record validation
   truthfully. Preserve Claude skills/settings and the initial graph-contact
   incident record. Distinguish static custom-agent validation from actual CLI
   browser execution and the durable Playwright journey suite.
4. Inspect npm's actual dependency graph and current advisory sources. Apply
   compatible fixes without --force. Align Node prerequisites with installed
   tool engines. Defer a breaking dependency migration only with exact affected
   versions, reachable-use analysis, and a bounded fix/validation plan.
5. Publish a current architecture/completion assessment and update the queue
   and residual ledger. Review all changed/untracked task files before staging.

## Acceptance and validation

- Given a new Claude or Codex developer, reading the entry files leads to the
  same current architecture and test commands, without broad capability fallbacks
  or obsolete suite/pipeline instructions.
- Given an old unchecked phase plan, its status notice identifies the current
  completion record and preserves historical implementation evidence.
- Given the existing owner worktree, migration leaves its original definitions,
  next-env change and unrelated files unchanged and unstaged.
- Given dependency changes, npm audit identifies exact remaining advisories;
  npm run check, focused affected tests, and the R7 full regression validate the
  selected versions. No live credentials, accounts or owner graph are contacted.
- Given the Codex artifacts, TOML/frontmatter and helper syntax validate; browser
  evidence is linked without claiming that the optional custom role was invoked.

Reviews use existing independent agents because fresh reviewer slots are
unavailable. The owner's sequential completion authorization overrides skill
confirmation/no-push pauses; commits remain scoped to reviewed task files.

## Review and evidence

The shared Claude/Codex guide, BMAD pointer, current architecture assessment and
historical status notices are complete. Workspace documents remain proposals.
All 66 Codex skill manifests, the browser TOML and helper syntax validate; the
optional custom role is not claimed as invoked. Six fake-command cases verify
the corrected manual browser readiness helper without using live ports.

Review corrected graph-unavailable deletion claims, WebSocket envelopes, unused
daily-budget/digest settings and overstated schedule self-test coverage. Budget
enforcement and ordinary schedule staleness detection have explicit follow-ups.
Dependency review found the unexported Sharp package.json import in the new guard;
it now uses the public native version API and the final required check passes.

Compatible updates and the scoped Sharp 0.35.4 override leave zero npm audit
advisories. Real compiled BGE fp32 generation passes online and in a second offline
process with temporary cache, 384-value/norm/repeatability assertions and synthetic
Sharp adapter checks. Node22.23.2/npm10.9.8 match deployment; npm11.7 ignored the
override, so the required dependency guard checks the actual installed result.
The shared/core build, required check, 59 API/ownership/web tests and 11 config
tests pass. The graph embedding test was excluded by the normal opt-in boundary;
no additional Neo4j run is claimed for R6. All 87 original definitions remain
unchanged. R7 owns final full/browser/production/container regression.

Evidence: /tmp/raven-r6-check-final.log, /tmp/raven-r6-focused.log,
/tmp/raven-r6-embedding-config-tests.log, /tmp/raven-r6-embeddings-final.log,
/tmp/raven-r6-audit-final.json, /tmp/raven-r6-helper-verification.log.
Two reused independent review perspectives and parent review were used.
